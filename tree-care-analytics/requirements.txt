# Application environment. Prefer keeping dbt in its own venv: dbt-snowflake
# pins certifi <2025.4.26, and a combined install drags this environment's CA
# bundle back with it. See WALKTHROUGH.md section 0.

# Layer 1: operational API
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0,<2.1
pymysql>=1.1
pydantic>=2.9
pydantic-settings>=2.6
alembic>=1.14

# Layer 3: analytical reads. Requires Python 3.10+.
#
# Pinned below 5.0.0 deliberately. The 5.x line (public preview, installs only
# with --pre) is rebuilt on a Rust core and adds snowflake.connector.aio, but
# dbt-snowflake and Snowflake's own libraries all declare <5.0.0 and cannot be
# installed alongside it.
#
# snowflake-sqlalchemy is absent by design, not by necessity: it accepts
# SQLAlchemy 2.0 fine (>=1.4.19, no upper bound). Analytical queries here are
# hand-written SQL aggregates, where an ORM adds nothing.
snowflake-connector-python>=4.2.0,<5.0.0
